function changeSum(id, priceValue){
    alert("here");
    /*var priceId = "#price" + id;
    var quantityId = "#quantity" + id;
    var changedValue = parseInt($(quantityId).html()) * priceValue;
    alert(changedValue);
    $(priceId).html(changedValue);*/
}